<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:label="parentsapp"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        tools:overrideLibrary="me.carda.awesome_notifications">

        <!-- Your other application components and settings -->

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Add the receiver tag with android:exported attribute -->
<!--        <receiver-->
<!--            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"-->
<!--            android:exported="false">-->
<!--            &lt;!&ndash; Add any other attributes as needed &ndash;&gt;-->
<!--        </receiver>-->
    </application>
</manifest>
